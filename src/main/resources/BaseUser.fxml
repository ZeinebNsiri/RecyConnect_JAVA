<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.image.*?>
<?import javafx.geometry.*?>
<?import com.jfoenix.controls.*?>


<BorderPane fx:id="rootBorderPane"
            xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="controllers.BaseUserController"
            stylesheets="@baseUser.css"
            prefWidth="1300" prefHeight="700">

    <!-- HEADER -->
    <top>
        <VBox spacing="0" style="-fx-background-radius: 10; -fx-padding: 10;">
            <HBox alignment="CENTER_LEFT" styleClass="top-bar" prefHeight="40" spacing="40" style="-fx-background-radius: 10;">
                <Label text="📞 +216 95 123 742" styleClass="contact-label"/>
                <Label text="|" styleClass="separator"/>
                <Label text="📧 RecyConnect@gmail.com" styleClass="contact-label"/>
                <Pane HBox.hgrow="ALWAYS"/>
                <Label text="Suivez-nous :   " styleClass="social-media"/>
            </HBox>

            <!-- Menu -->
            <HBox spacing="40" alignment="CENTER_LEFT" styleClass="menu-bar" prefHeight="80">
                <ImageView fitHeight="50" fitWidth="50">
                    <Image url="mainlogo.png"/>
                </ImageView>

                <!-- Navigation Links -->
                <Label text="Accueil" styleClass="nav-link"/>
                <Label text="Nos produits" styleClass="nav-link"/>
                <Label text="Évènements" styleClass="nav-link"/>
                <Label text="Workshops" styleClass="nav-link"/>
                <Label text="Forum" styleClass="nav-link"/>

                <Pane HBox.hgrow="ALWAYS"/>

                <!-- Icons -->
                <Label fx:id="notificationIcon" text="🔔" styleClass="icon"/>
                <Label text="🛒" styleClass="icon"/>

                <!-- Profile Dropdown -->
                <MenuButton styleClass="user-menu">
                    <graphic>
                        <HBox spacing="10" alignment="CENTER">
                            <ImageView fitWidth="30" fitHeight="30" style="-fx-clip-shape: circle; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.3), 4, 0, 0, 1); -fx-border-radius: 15; -fx-background-radius: 15;">
                                <Image url="mainlogo.png" />
                            </ImageView>
                            <Label text="Mohamed Aziz Zouari" styleClass="profile-name"/>
                        </HBox>
                    </graphic>
                    <items>
                        <MenuItem text="👤 Mon Profil" />
                        <MenuItem text="📑 Mes commandes" />
                        <MenuItem text="🚪 Se Déconnecter" />
                    </items>
                </MenuButton>

            </HBox>
        </VBox>
    </top>

    <!-- BODY DYNAMIQUE -->
    <center>
        <VBox alignment="CENTER" spacing="20">
            <Label text="Bienvenue sur RecyConnect" styleClass="welcome-title"/>
            <Label text="Découvrez nos produits et participez à nos événements !" styleClass="welcome-subtitle"/>
        </VBox>
    </center>

</BorderPane>
